# Tecnolight - Tests

## Backend Tests

```bash
cd backend
npm test
```

Cobertura: health, auth (login + 2FA), products CRUD, projects, contact, instagram, upload.

## Frontend Tests (pendiente de instalar dependencias)

```bash
cd frontend
npm test
```

Requiere: `jest`, `@testing-library/react`, `@testing-library/jest-dom`.
